services:
  prometheus:
    image: prom/prometheus:v2.52.0
    ports:
      - "9090:9090"
    network_mode: host
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus

volumes:
  prometheus_data: